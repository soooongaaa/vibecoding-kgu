# 12간지 슬롯머신 SPEC

- 프로젝트: vibecoding-kgu (Next.js App Router + Supabase, 12간지 미니게임 허브)
- 작업 브랜치: `feature/game-dbqp8009-b`
- 작성일: 2026-08-19

## 1. 목표

팀의 12간지 미니게임 허브(`src/lib/game/zoo-games.ts` 기반)에 들어갈 **확률형 재미 슬롯머신**을 추가한다.

## 2. 사용자

로그인 여부와 무관하게 **누구나 플레이** 가능.

## 3. 문제 / 가치

버튼 하나로 즉시 즐길 수 있는 짧고 확률적인 재미 요소를 제공한다. 기록/보상 목적이 아니라 그 자리에서 결과를 확인하고 끝나는 가벼운 게임이다.

## 4. 범위

- 릴(칸) **3개**로 구성된 전통적인 슬롯머신
- 심볼은 팀 허브(`zoo-games.ts`)와 동일한 **12간지 동물 이모지 12종**을 사용: 🐭 🐮 🐯 🐰 🐲 🐍 🐴 🐑 🐵 🐔 🐶 🐷
- 지원 기기: **PC(데스크톱) 전용**. 모바일 반응형은 이번 범위에 포함하지 않는다
  - ⚠️ 팀 README의 "게임 최소 완료 기준"에는 모바일 대응이 명시되어 있으나, 사용자가 이번에도 PC 전용으로 진행하기로 **의도적으로 결정**함 (확인됨, [확인 필요] 아님)
- 회원가입/로그인 기능은 기존 것을 그대로 사용 (신규 구현 없음)

## 5. 게임 사양

### 5.1 핵심 플레이

- 버튼을 누르면 릴 3개가 **짧게 도는 애니메이션** 후 각각 심볼 하나로 멈춘다
- 3칸의 심볼이 **모두 같으면 성공(당첨)**, 아니면 실패
- 스핀 횟수 **무제한** — 코인이나 횟수 소모 개념 없이 언제든 다시 누를 수 있다

### 5.2 확률 구현 방식 (중요)

릴마다 독립적으로 무작위 심볼을 뽑으면 심볼 개수(12개)에 따라 당첨 확률이 정해져 버려(1/12² ≈ 0.69%), 정확히 10%를 맞출 수 없다. 따라서:

1. 스핀할 때마다 **먼저 "성공 여부"를 10% 확률로 결정**한다 (예: `Math.random() < 0.1`)
2. **성공**이면: 12종 중 무작위 심볼 1개를 골라 3칸 모두 동일하게 채운다
3. **실패**면: 3칸을 각각 완전히 무작위로 채운다 (2칸만 같은 것은 허용). 단, 완전 무작위 결과가 우연히 3칸 모두 같아져 버리면 실패 결과가 당첨으로 보이지 않도록 **다시 뽑는다** (3칸이 다르게 나올 때까지 재추첨)

이 방식으로 심볼 개수와 무관하게 항상 정확히 10% 당첨 확률이 보장된다.

### 5.3 결과 표시 (저장 없음)

- 당첨/실패 결과를 화면에 표시하고 끝낸다 (요리 미니게임과 동일하게 **Supabase 저장 없음**, 새로고침하면 사라짐)
- 당첨 기록·통계 기능은 이번 범위에 포함하지 않는다

### 5.4 비주얼 테마 (2차 수정됨)

~~처음엔 붉은 옻칠+금장식 운세방 테마, 이후 요리게임과 통일한 핑크 상점+마스코트 테마~~를 거쳐, 사용자가 세 번째 참고 이미지(플랫 아이콘 스타일의 클래식 카지노 슬롯머신 — 은회색 상단 호퍼, 라벤더그레이 릴 창, 진한 차콜 넥, 코랄핑크 하단 받침+동전 트레이, 오른쪽 빨간 손잡이 레버, 회색 다리)를 제시해 **마스코트·말풍선·상점 간판을 모두 제거하고, 기계 자체를 굵은 다크 네이비 테두리의 플랫 일러스트 슬롯머신으로 재구성**했다. 릴 심볼은 계속 12간지 이모지를 사용하고, 각 릴 모서리에 작은 파스텔 색 장식 점(pip)을 추가했다. 페이지 배경은 연한 라벤더그레이 단색으로 단순화. 상단 타이틀 "두근두근 띠뽑기"만 유지.

### 5.5 조작 방식 (수정됨)

~~중앙의 둥근 "뽑기" 버튼~~ 대신, **머신 오른쪽에 달린 막대(레버)를 당기는 모션**으로 스핀을 시작한다. 막대를 클릭하면 아래로 당겨졌다가 스프링처럼 튀어 올라오는 애니메이션과 함께 릴이 돈다.

## 6. 팀 공식 구조 반영 (중요 — 이전 요리게임과 다른 점)

`main`/`dev`에 이미 팀이 만들어 둔 12간지 미니게임 허브 구조를 그대로 따른다:

- 실제 구현 위치: `src/games/<slug>/` (요리게임 때 썼던 `src/app/game/` 방식이 아님)
- 라우트: `src/app/games/<slug>/page.tsx`를 새로 만들어 위 컴포넌트를 렌더링
- `src/lib/game/zoo-games.ts`에서 배정된 동물 항목을 찾아 `available: true`로 바꾸고 `href`를 위 라우트로 연결
- 팀 공용 `feature/game-common` 스캐폴드는 아직 만들어지지 않았음 확인됨 — 현재는 참고할 공통 컴포넌트가 없으므로 이번 게임만의 UI로 자체 구현

## 7. 제약

- **마감**: 오늘(2026-08-19) 안에 완료 (팀 하루 완성 목표와 동일)
- **스택**: 기존 스택 유지 — Next.js App Router + TypeScript. 새 라이브러리·새 Supabase 테이블 도입 없음
- **작업 범위**: `feature/game-dbqp8009-b` 브랜치에서만 작업, 원칙적으로 자기 게임 폴더(`src/games/<slug>/`)만 수정. `zoo-games.ts`처럼 공용 파일을 건드릴 때는 해당 동물 항목만 최소 수정
- **Git 운용**: 모든 작업 결과물은 **로컬 파일 변경으로만** 반영한다. `git add` / `git commit` / `git push` 등 모든 Git 명령어는 사용자가 그 순간에 명시적으로 요청하기 전까지 실행하지 않는다
- **lint/build 검증**: 이 작업 환경에는 Node.js가 없어 `npm run lint`/`npm run build`를 대신 실행해 줄 수 없다. **사용자가 Node.js가 있는 다른 환경에서 직접 실행하고 결과를 확인**한다

## 8. 성공 기준 (완료 정의)

로컬에서 `npm run dev`로 실행했을 때 다음이 모두 동작하면 완료로 본다:

- [ ] 버튼을 누르면 릴이 짧게 도는 애니메이션 후 멈춘다
- [ ] 3칸이 모두 같으면 당첨, 아니면 실패로 표시된다
- [ ] 여러 번 반복 시 체감상 대략 10% 정도의 확률로 당첨된다 (정확한 확률 코드는 5.2 방식을 따름)
- [ ] 버튼을 계속 눌러 무제한으로 다시 스핀할 수 있다
- [ ] 로그인 여부와 무관하게 플레이 가능하다

`npm run lint` / `npm run build` 통과 여부는 사용자가 별도 환경에서 직접 확인하며, 이번 완료 기준의 자동 검증 항목에는 포함하지 않는다.

## 9. [확인 필요] — 아직 정해지지 않은 것

- **동물/slug 배정**: `zoo-games.ts`에 아직 게임이 없는 동물 10개(쥐/소/토끼/용/뱀/말/양/닭/개/돼지) 중 이 슬롯머신을 어느 동물 자리에 넣을지 **팀과 협의되지 않음**. 팀 협의 전까지 임시로 `zodiac-slot`이라는 slug로 구현해 두었다 (`src/games/zodiac-slot/`, 라우트 `src/app/games/zodiac-slot/page.tsx`). 협의 후 실제 동물이 정해지면:
  - 폴더명을 `src/games/<확정된-slug>/`로 변경하고 라우트도 `src/app/games/<확정된-slug>/page.tsx`로 옮긴다
  - `zoo-games.ts`의 해당 동물 항목을 찾아 `gameName`, `href`, `available: true`로 갱신한다 (이 SPEC 작성 시점에는 아직 미수정)
  - 그 전까지는 `/games/zodiac-slot` 경로로 독립적으로만 접근 가능하고, 허브(`/`)에서는 연결되지 않는다
- 결과 화면의 정확한 문구·문안
- 심볼별 세부 표시 스타일(이모지 크기, 릴 애니메이션 디테일 등)은 구현 단계에서 결정
