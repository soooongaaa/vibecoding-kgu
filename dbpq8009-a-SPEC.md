# 요리 미니게임 SPEC

- 프로젝트: vibecoding-kgu (Next.js App Router + Supabase)
- 작업 브랜치: `feature/game-dbqp8009-a`
- 작성일: 2026-08-19

## 1. 목표

vibecoding-kgu 팀 앱에 **로그인 사용자의 재방문(리텐션)을 유도하는 재미 요소**로 요리 미니게임을 추가한다.

## 2. 사용자

- 로그인 여부와 무관하게 **누구나 플레이** 가능
- (v1에는 기록 저장 기능이 없어 로그인 여부가 플레이에 영향을 주지 않음 — [변경 이력](#9-변경-이력) 참고)

## 3. 문제 / 가치

사용자가 앱에서 즐길 수 있는 재미 요소를 제공한다. (재방문 유도용 "개인 최고기록" 저장은 v1 범위에서 제외 — 9번 참고)

## 4. 범위

- 새 라우트 1개만 생성 (예: `/game`). 기존 메인 페이지(`src/app/page.tsx`)에 진입 링크/버튼은 **추가하지 않음**
- 지원 기기: **PC(데스크톱) 전용**. 모바일 반응형 대응은 이번 범위에 포함하지 않음
- 회원가입/로그인 자체 기능은 기존 것을 그대로 사용 (신규 구현 없음)

## 5. 게임 사양

### 5.1 핵심 플레이

- 재료/단계를 **클릭만으로 순서대로 선택**하는 매칭 퍼즐
- 화면에는 **정답 재료 + 함정(관계없는) 재료**가 함께 보임
- 화면에 보이는 버튼(정답+함정)의 **배치 순서는 매 시도마다 무작위로 셔플**
- 재료/함정 항목은 **이모지 아이콘 버튼**으로 표현 (별도 이미지 파일 없음)

### 5.2 난이도 3단계 (고정 레시피)

각 단계는 '시작' 버튼을 누르면 60초 카운트가 시작된다.

| 단계 | 요리 | 정답 순서 | 함정 재료 |
|---|---|---|---|
| 1 | 핫도그 | 핫도그 빵 → 소세지 → 머스타드 | 케첩, 치즈 |
| 2 | 샌드위치 | 빵 → 딸기잼 → 상추 → 햄 → 빵 | 블루베리잼, 베이컨, 치즈 |
| 3 | 김밥 | 김 → 밥 → 단무지 → 우엉 → 당근 → 오이 | 치즈, 계란, 참치 |

### 5.3 진행 / 종료 규칙

- 각 단계 제한시간: **60초 (단계마다 각각 새로 카운트)**
- 종료(그 단계 실패) 조건: **제한시간 초과 OR 함정 재료를 클릭하는 순간, 둘 중 먼저 오는 것**
- 특정 단계에서 실패하면 **1단계부터 게임 전체를 재시작** (해당 단계만 재시도하는 것이 아님)
- 3단계를 모두 클리어하면 **게임 성공**으로 처리하고, 1단계 시작 시점부터 3단계 완료 시점까지의 **총 소요시간**을 확정한다

### 5.4 결과 표시 (v1: 저장 없음)

- 3단계를 모두 클리어하면 총 소요시간을 화면에 표시하고 끝낸다
- **Supabase 저장/최고기록 기능은 v1 범위에서 제외한다** (새로고침하면 결과는 사라짐) — 9번 변경 이력 참고
- 재시도 횟수 제한 없음 (실패하면 언제든 1단계부터 다시 시작 가능)

### 5.5 화면 문구 (결정됨)

- 함정 클릭 실패: `❌ 함정 재료예요! 처음부터 다시 도전해 보세요.`
- 시간 초과 실패: `⏰ 시간 초과! 처음부터 다시 도전해 보세요.`
- 단계 클리어(1→2, 2→3): `✅ N단계 완료! 다음 단계로 이동합니다.`
- 최종 클리어: `🎉 미션 클리어! 총 소요시간: {총 소요시간}초`

## 6. 제약

- **마감**: 오늘(2026-08-19) 안에 완료
- **스택**: 기존 스택 유지 — Next.js App Router + TypeScript. 새 라이브러리·새 Supabase 테이블 도입 없음 (v1은 순수 클라이언트 상태로만 동작)
- **작업 범위**: `feature/game-dbqp8009-a` 브랜치에서만 작업, 요청받은 기능과 무관한 파일은 임의 수정하지 않음
- **Git 운용**: 모든 작업 결과물은 **로컬 파일 변경으로만** 반영한다. `git add` / `git commit` / `git push` 등 모든 Git 명령어는 사용자가 그 순간에 명시적으로 요청하기 전까지 실행하지 않는다 (GitHub 업로드는 명시적 요청이 있을 때만)

## 7. 성공 기준 (완료 정의)

로컬에서 `npm run dev`로 실행했을 때 다음이 모두 동작하면 완료로 본다:

- [ ] 1→2→3단계 순서대로 플레이 가능
- [ ] 함정 클릭 또는 60초 초과 시 실패 → 1단계부터 재시작되는 것 확인
- [ ] 3단계 모두 클리어 시 총 소요시간이 표시됨
- [ ] 로그인 여부와 무관하게 게임 플레이 자체는 가능함

빌드(`npm run build`)/린트 통과는 이번 완료 기준에 **포함하지 않음**.

## 8. [확인 필요] — 아직 정해지지 않은 것

없음 (아래 9번 변경 이력에서 모두 확정됨)

## 9. 변경 이력

- 2026-08-19: 재료 버튼은 **이모지 + 짧은 한글 라벨**을 함께 표시하기로 확정 (예: 🍅 케첩)
- 2026-08-19: **Supabase 최고기록 저장 기능을 v1 범위에서 제외**하기로 결정 (사용자 요청). 결과는 그 판에서만 화면에 표시하고 저장하지 않는다. 추후 필요하면 `game_best_times` 테이블(user_id, best_time_ms, updated_at) + RLS 방식으로 별도 작업 진행
- 2026-08-19: 사용자가 참고 이미지(핑크톤 아기자기한 요리 게임)를 제공, 다음 3가지를 추가로 확정:
  - **비주얼 테마**: 핑크·크림·골드 배색, "Jua"/"Gowun Dodum" 폰트, 왕관 쓴 마스코트 셰프 캐릭터 + 상태별 말풍선, 장식용 냄비. 이미지 속 특정 브랜드명/캐릭터는 그대로 복제하지 않고 스타일만 참고해 자체 디자인("미니 셰프의 주방")으로 구성
  - **정답 미리보기**: 각 단계 시작 전 함정 없이 정답 재료 순서만 화살표로 이어서 **3초간** 보여준 뒤(60초 타이머는 이때 아직 시작 안 함), 함정이 섞인 채로 셔플된 보드가 나타나며 60초 타이머 시작
  - **재료 쌓기 연출**: 정답 재료를 클릭할 때마다 화면 속 접시 위에 그 재료가 나타나고, 이후 클릭한 재료는 그 위에 겹쳐서 쌓임 (실패해도 그때까지 쌓은 모습은 유지해서 보여줌)
