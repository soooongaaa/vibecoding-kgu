# CONSPEC — 편의점 상품 진열 게임

작성일: 2026-08-19
브랜치: feature/game-woojoo22kr-a (게임 A)
작성 방식: 인터뷰 기반, 확정되지 않은 항목은 [확인 필요]로 표시하고 추측하지 않음

## 1. 목표 (Why)
- vibecoding-kgu 팀 해커톤/포트폴리오용 데모 게임. 6명이 하루 안에 실제로 작동하는 결과물을 완성한다는 팀 공통 목표에 맞춰, 완벽한 설계보다 처음부터 끝까지 실행되는 작은 결과물을 우선한다.

## 2. 사용자 (Who)
- 같은 팀 팀원들 (코드/데모 확인용)
- 배포된 사이트를 통해 접속하는 일반 사용자

## 3. 핵심 문제 / 게임플레이 (What — 재미의 본질)
- 장르: 드래그앤드롭 배치 퍼즐
- 판정 규칙: **카테고리 위치 매칭만** 확인한다. 수량, 유통기한(선입선출), 진열 방향 등 실제 MD 규칙은 반영하지 않는다.
- 피드백 타이밍: 상품을 칸에 놓는 즉시 O/X로 시각 피드백을 준다.
- 실패 상태: 없음. 틀리면 다시 배치를 시도할 수 있고 페널티는 없다.
- 종료 조건: 모든 상품을 올바른 카테고리 칸에 배치하면 종료된다.
- 결과 화면: 점수 없이 "완료" 메시지만 표시한다.
- 점수/기록 저장: 세션 내에서만 표시하고, Supabase 등에 영구 저장하지 않는다.
- 재시작("다시하기"): 누를 때마다 상품 배치를 무작위로 셔플해서 새로 시작한다.

## 4. 범위 (Scope — MVP)
- 단일 스테이지(레벨/난이도 단계 없음)
- 카테고리 4개: 음료 / 과자 / 라면류 / 생활용품
- 상품 개수: 10~12개
- 비주얼 자산: 사진이 아닌 간단한 벡터 이미지(아이콘/일러스트)를 생성해서 사용
- 게임 이름 / 폴더명 / URL slug: [확인 필요] — 사용자가 추후 직접 정하기로 함

## 5. 제약 (Constraints)

### 팀 공통 규칙 (docs/MAIN.md, DEV.md, FEATURE.md, README, origin/dev의 CLAUDE.md 기준)
- Git 흐름: `feature/game-woojoo22kr-a → dev → main`. 팀원은 `dev`/`main`에 직접 push하지 않으며, PR은 팀장만 병합한다.
- 이 게임은 "게임 A"로서 `feature/game-woojoo22kr-a` 브랜치에서만 작업하고, 게임 B(`feature/game-woojoo22kr-b`)와 코드를 섞지 않는다.
- 담당자는 원칙적으로 자기 게임 폴더만 수정한다. 공용 CSS, 전역 파일, `package.json` 등 공용 파일을 바꿔야 하면 먼저 팀장과 협의한다.
- 스택은 고정: Next.js(App Router) + TypeScript, 스타일은 일반 CSS(Tailwind 등 미사용), 별도 상태관리 라이브러리 없음(순수 React state).
- 배포: Vercel, `main` 브랜치 자동 배포.

### 이번 게임 관련 미확정 항목 (모두 [확인 필요])
- **드래그앤드롭 구현 방식**: 새 라이브러리(예: dnd-kit) 추가는 공용 `package.json` 변경이 필요해 팀장 협의가 필수. 사용자는 "팀장에게 먼저 물어본다"고 답함 → 팀장 확인 결과 대기. (대안: 순수 React state + HTML5 네이티브 Drag API로 라이브러리 없이 구현 가능하나, 이 경우 모바일 터치 드래그를 직접 구현해야 함)
- **브랜치 최신화 여부**: 현재 `feature/game-woojoo22kr-a`는 `origin/dev`의 최신 커밋(12개 미니게임 협업 규칙, `src/games/<slug>/` 폴더 구조, `.claude/hooks/` 안전장치, `CLAUDE.md` 추가)이 반영되기 **이전** 시점에서 분기되어 있음. 최신 `dev` 기준으로 브랜치를 다시 시작/리베이스해야 하는지 팀장과 확인 필요.
- **게임 코드 배치 경로**: 팀 문서(README/CLAUDE.md, `origin/dev` 기준)상 컨벤션은 `src/games/<slug>/`이나, 실제로 이 구조로 구현된 게임이 아직 하나도 없고 Next.js App Router 라우트와 어떻게 연결되는지도 코드로 확인된 바 없음. 팀장 확인 필요.
- **공통 레이어(`feature/game-common`)**: 공통 레이아웃/시작·재시작 버튼/결과 모달을 담을 이 브랜치가 아직 존재하지 않음. 이 게임을 먼저 독자적으로 구현할지, 공통 레이어를 기다릴지 확인 필요.

## 6. 성공 기준 (Definition of Done)
팀 공통 기준(README/CLAUDE.md)을 그대로 적용한다:
- [ ] 배포된 사이트에서 접속 가능
- [ ] 시작 → 플레이(4개 카테고리에 10~12개 상품 드래그 배치) → 완료 메시지 → 다시하기(무작위 셔플) 흐름이 실제로 동작
- [ ] 상품을 칸에 놓는 즉시 정오답 피드백 표시
- [ ] 모바일 터치로 드래그 조작 가능
- [ ] 콘솔에 치명적 오류 없음
- [ ] `npm run lint`, `npm run build` 통과
- [ ] PR에 설명, 실행 방법, 테스트 결과, 스크린샷/캡처 첨부

사용자 개인이 추가로 제시한 성공 기준은 없음 — "하루 안에 작동하는 결과물"이라는 팀 목표 자체가 성공 기준.
